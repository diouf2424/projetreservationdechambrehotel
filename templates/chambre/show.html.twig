{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Typachambre</th>
                <td>{{ chambre.typachambre }}</td>
            </tr>
            <tr>
                <th>Nombrelit</th>
                <td>{{ chambre.nombrelit }}</td>
            </tr>
            <tr>
                <th>Typelit</th>
                <td>{{ chambre.typelit }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ chambre.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chambre_index') }}">back to list</a>

    <a href="{{ path('chambre_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
